%p{class: interview_question.visible? ? "visible" : "unvisible" }
  = interview_question.id
  = interview_question.question_text
%p.tools
  = link_to "bearbeiten", edit_admins_interview_question_path(interview_question)
  = link_to "löschen", admins_interview_question_path(interview_question), :method => :delete, confirm: "Sicher?"
  
%p= interview_question.interview_answers.present? ? ( render interview_question.interview_answers ) : "Keine Antworten vorhanden"
%hr